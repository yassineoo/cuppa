
const commandesLogic = {
    translate: (instructions: Map<string, string>): string[] => {
        const commande = "dispense" 
        instructions.forEach((value, key) => {

        })
        return ["ins1", "ins2"]
    }
}

export default commandesLogic